	develop_templateStateID_templateResourceType_deposits = {

		icon = templateResourceType

		allowed = {

		}

		highlight_states = {
			highlight_state_targets = {
				state = templateStateID
			}
		}

		available = {
			has_tech = excavationtemplateExcavationLevel
			num_of_civilian_factories_available_for_projects > templateFactoryCost
			owns_state = templateStateID
			controls_state = templateStateID
		}

		visible = {
			owns_state = templateStateID
			controls_state = templateStateID
			templateStateID = {
				NOT = {
					has_state_flag = has_templateStateID_templateResourceType_developed
				}
			}
		}

		fire_only_once = yes

		cost = templatePPCost
		days_remove = 60

		modifier = {
			civilian_factory_use = templateFactoryCost
		}

		ai_will_do = {
			base = 0.3
			modifier = {
				factor = 0
				AND = { #Too low MP we need to save PP for the laws
					has_manpower < 10000
					NOT = { has_idea = scraping_the_barrel }
				}
			}
			modifier = {  # Too low PP
				factor = 0.5
					has_political_power < 80
			}
			modifier = { #We have plenty of PP, avanti!
				factor = 3
				has_political_power > 200
			}
			modifier = { #Running out of the stuff, need more!
				factor = 5
				has_resources_in_country = {
					resource = templateResourceType
					amount < 1
				}
			}
		}
		remove_effect = {
			templateStateID = { set_state_flag = has_templateStateID_templateResource_developed }
			templateStateID = {
				add_resource = {
					type = templateResourceType
					amount = templateResourceAmount
				}
			}
		}
	}
	